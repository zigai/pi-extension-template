import assert from "node:assert/strict";
import test from "node:test";

import { {{ test_import_names|join(", ") }} } from "../src/index.ts";

test("exports starter metadata", () => {
{%- for name, value in test_expected_exports %}
    assert.equal({{ name }}, {{ value|tojson }});
{%- endfor %}
});
