# {{ title_name }}
{% if description %}
{{ description }}
{% endif %}
## Install

```sh
pi install npm:{{ package_name }}
```

## Usage

Start Pi normally. This scaffold prepares configuration before the first agent turn; it does not yet add tools or commands.

<!-- pi-extension-settings:start -->
## Configuration

Run `npm run config:generate` to generate this section.
<!-- pi-extension-settings:end -->

Set `enabled` to `false` to disable extension behavior, then reload Pi. Use `pi config` to prevent loading the extension entirely.

## Development

```sh
just setup
just coverage
```

Edit `src/settings-input.ts` and run `npm run config:generate` to update settings.
`src/index.ts` is the direct TypeScript entry; `npm run verify` checks source, tests, and the npm artifact.

{% if copyright_license != "None" %}
## License

[{{ copyright_license }}](LICENSE)
{% endif %}
