# {{ title_name }}
{% if description %}
{{ description }}
{% endif %}
## Install

```sh
pi install git:{{ github_install_source }}
```

For this private package on this machine, load the local working copy from Pi settings:

```json
{
  "packages": [{{ destination_path|tojson }}]
}
```

## Extension

This package starts with an empty Pi extension in `src/index.ts`.
{% if extension_settings %}
<!-- pi-extension-settings:start -->
## Configuration

Global settings are stored in `~/.pi/agent/extension-settings/{{ repo_name }}.json`.

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `enabled` | boolean | `true` | Enable the extension. |

```json
{
  "$schema": "./schemas/{{ repo_name }}.schema.json",
  "enabled": true
}
```
<!-- pi-extension-settings:end -->
{% endif %}
## Development

```sh
npm install
just check
just coverage
```
