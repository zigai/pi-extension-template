# {{ title_name }}
{% if description %}
{{ description }}
{% endif %}
## Install

```sh
pi install npm:{{ package_name }}
```

## Usage

After installation, start Pi normally; no additional activation step is required.

<!-- pi-extension-settings:start -->
## Configuration

Run `npm run config:generate` to generate this section.
<!-- pi-extension-settings:end -->

`enabled` is the extension-owned global/project behavior switch. When adding behavior, resolve settings once per session at first use and return from every disabled callback without side effects. Reload after changing it. Use `pi config` for the zero-load hard switch when Pi should not import or register the extension at all.

## Development

```sh
just setup
just coverage
```

The package uses {{ "a bundled `dist/index.ts` production entry" if build_mode == "bundle" else "the direct `src/index.ts` entry" }}. `npm run verify` runs source checks and the focused npm artifact check.

{% if copyright_license != "None" %}
## License

[{{ copyright_license }}](LICENSE)
{% endif %}
