import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

/** Register the {{ title_name }} Pi extension. */
export default function extension(pi: ExtensionAPI): void {
    pi.on("session_start", () => {
        // Reset session-owned activation, settings, cancellation, and resources here. Keep this
        // startup-critical callback free of settings I/O unless the feature must begin now.
    });

    pi.on("session_shutdown", () => {
        // Abort and dispose session-owned work, then clear its activation and settings state.
    });

    // Register tools, commands, renderers, and other Pi resources synchronously here. When adding
    // behavior, follow AGENTS.md's exactly-once activation pattern from every possible first-use
    // callback and return without side effects when resolved settings have enabled: false.
}
